name: legal_metrology_field
description: Offline-first field verification client
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.1
  shared_preferences: ^2.2.3
  geolocator: ^13.0.1
  image_picker: ^1.1.2

flutter:
  uses-material-design: true


